import 'package:flutter/material.dart';

class BreathingTrackerScreen extends StatelessWidget {
  const BreathingTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BreathingTrackerScreen'),
    );
  }
}
